French:
"Ceci est mon projet de machine learning de M2. L'objectif est de calculer l'énergie d'atomisation de molécule. Il faut paramétrer le chemin d'accès au fichier roboBohr.csv. Certains hyperparamètres sont très longs à calculer, il ne faut pas hésiter à couper la liste en deux ou à prendre les résultats écrits ci-dessous."

English:
"This is my M2 machine learning project. The goal is to calculate the atomization energy of molecules. You need to set the path to the roboBohr.csv file. Some hyperparameters take a long time to compute, so don't hesitate to split the list in two or use the results written below."